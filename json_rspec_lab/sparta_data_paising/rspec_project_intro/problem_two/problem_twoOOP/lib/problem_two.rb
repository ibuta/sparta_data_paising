class Problem_two
	attr_accessor :problem_two

	def inialize
		@problem_two = [1,2]
	end

	def sum (x, num)
		(x + num)
	end

	def problem_two_iterator (range_from, range_to)
		(range_from;range_to) while < |totalmax|
			x + num = sum							
			 
			x.each { |num| sum += num if num.even? }		
			if sum.even? 
				problem_two << 'The sum of even numbers is #{sum}'
			end 

			if sum.odd?				
				@problem_two_array << 'should correctly apply others outside of the even range'
			end		
		end
	end
end 